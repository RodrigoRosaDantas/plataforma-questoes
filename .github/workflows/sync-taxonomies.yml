name: Sincronizar taxonomias dos editais

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/sync-editorial-taxonomies.mjs"
      - ".github/workflows/sync-taxonomies.yml"
  schedule:
    - cron: "23 * * * *"

permissions:
  contents: write

concurrency:
  group: sync-editorial-taxonomies
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Sincronizar taxonomias sanitizadas
        run: node scripts/sync-editorial-taxonomies.mjs
      - name: Validar plataforma
        run: npm run validate
      - name: Commitar edital derivado
        shell: bash
        run: |
          if git diff --quiet -- data/editais.json; then
            echo "Sem alterações nas taxonomias."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/editais.json
          git commit -m "data: sincronizar taxonomias dos editais"
          git pull --rebase origin main
          git push origin HEAD:main
