name: Otimizar facetas em memória

on:
  push:
    paths:
      - '.github/workflows/apply-facet-memory-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Aplicar patch seguro
        run: node scripts/apply-facet-memory-fix.mjs
      - name: Validar código
        run: |
          node --check assets/app.js
          node --check assets/ux-enhancements.js
          node --check scripts/check-regressions.mjs
          npm run validate
      - name: Commitar correção
        run: |
          if git diff --quiet; then
            echo 'Nenhuma alteração necessária.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/app.js assets/ux-enhancements.js scripts/check-regressions.mjs service-worker.js
          git commit -m 'perf: reutilizar questões carregadas nas facetas'
          git push
